type: pages
label: Network
parent: site.find("network")
sortBy: from desc

layout: table
image:
   query: page.cover.toFile
columns:
  location: true
    label: Location
template: organisation
empty: No organisations yet
