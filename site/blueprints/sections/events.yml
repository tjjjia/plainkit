type: pages
label: Calendar
parent: site.find("calendar")
sortBy: from desc
layout: table
image:
   query: page.cover.toFile
columns:
  datefrom: true
    label: Date from
  dateto: true
    label: Date to
  isallday: true
    label: All day?
    width: 1/12
  organisationinternal: true
    label: Organisation
  location: true
    label: Venue
template: event
empty: No events yet
